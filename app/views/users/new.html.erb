<div class="hero">
  <p class="large brandon">heirloom cutlery is coming</p>
  <p class="small brandon">chef quality cutlery<br>at a price you and<br>your wallet will love.</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>